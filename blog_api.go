package main

func UploadImage() {
	/*
		resp, err := http.PostForm("http://10.1.1.146:8080/api/image/upload",
			url.Values{"imgs": {path}})
		if err != nil {
			// TODO: if dir exists send response to client
			fmt.Println("createDir:", err)
			continue
		}
	*/
}
